import React from "react";
import LoginForm from "../pages/LoginForm";   // Importing the LoginForm component

const Login = () => {
  return (
    <div>
      <LoginForm />
    </div>
  );
};

export default Login;
